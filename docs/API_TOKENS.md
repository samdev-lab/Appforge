# AppForge Production API Token Management

## 1. Cryptographic Security
Tokens are stored as irreversible SHA-256 hashes. Raw secrets are displayed only once.

## 2. Scopes
CUSTOMER_READ, CUSTOMER_WRITE, APPLICATION_READ, APPLICATION_WRITE, INTEGRATION_EXECUTE, etc.
