# AppForge Third-Party Integration Onboarding

## 1. Connection Wizard
Connect external REST endpoints with API Key, Bearer Token, Basic Auth, or OAuth 2.0.

## 2. Credential Security
All secrets are stored in `AppForgeCredentialVault` and masked during operational logging.
