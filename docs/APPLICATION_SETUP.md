# AppForge Application Setup Framework

## 1. Declarative Setup Manifests
Each application (CRM, CSM, SPM, FSM, Resource Management, Bulk Catalog, ITSM) defines required and optional setup steps.

## 2. Readiness Criteria
Applications transition to `READY` state only when all required setup steps are validated and persisted.
