# AppForge Customer Application Lifecycle

## 1. State Machine
AVAILABLE -> TRIAL -> SUBSCRIBED -> INSTALLING -> INSTALLED -> ACTIVE -> SUSPENDED -> UPGRADE_AVAILABLE -> UPGRADING -> UNINSTALLING -> UNINSTALLED.

## 2. Server-Side Security
Invalid transitions are strictly rejected by the server-side lifecycle engine.
